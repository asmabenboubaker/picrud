{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Mot_passe_utili</th>
                <td>{{ utilisateur.motPasseUtili }}</td>
            </tr>
            <tr>
                <th>Nom_utili</th>
                <td>{{ utilisateur.nomUtili }}</td>
            </tr>
            <tr>
                <th>Tel_utili</th>
                <td>{{ utilisateur.telUtili }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ utilisateur.role }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ utilisateur.type }}</td>
            </tr>
            <tr>
                <th>Nbre_place</th>
                <td>{{ utilisateur.nbrePlace }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ utilisateur.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('carte_fidelite_index') }}">back to list</a>

    <a href="{{ path('carte_fidelite_edit', {'id': utilisateur.id}) }}">edit</a>

    {{ include('carte_fidelite/_delete_form.html.twig') }}
{% endblock %}
